network = model.createNetwork([
	layers.DenseLayer(input_shape),
	layers.DenseLayer(24, activation='relu', weights_initializer='zeros'),
	layers.DenseLayer(24, activation='relu', weights_initializer='zeros'),
	layers.DenseLayer(output_shape, activation='softmax', weights_initializer='zeros')
])

model.fit(network, data_train, data_valid, loss='classCrossentropy', optimizer="adam", learning_rate=0.1, epochs=20, batch_size=128, plot=True, print_every=100, decay=1e-4)
