network = model.createNetwork([
	layers.DenseLayer(input_shape),
	layers.DenseLayer(128, activation='relu', weights_initializer='random'),
	layers.DenseLayer(128, activation='relu', weights_initializer='random'),
	layers.DenseLayer(output_shape, activation='softmax', weights_initializer='random')
])

model.fit(network, data_train, data_valid, loss='classCrossentropy', learning_rate=0.001, batch_size=128, epochs=1, optimizer="adam", plot=False)
