network = model.createNetwork([
	layers.DenseLayer(input_shape),
	layers.DenseLayer(64, activation='relu', weights_initializer='random'),
	layers.DenseLayer(output_shape, activation='sigmoid', weights_initializer='random')
])

model.fit(network, data_train, data_valid, loss='binaryCrossentropy', optimizer="adam", learning_rate=0.001, epochs=1, plot=False, print_every=100, decay=5e-7)
